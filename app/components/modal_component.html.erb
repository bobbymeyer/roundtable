<%= turbo_frame_tag "modal" do %>
  <div data-controller="modal" data-action="click@window->modal#hideModalBg" id='modal-bg' class="absolute left-0 right-0 top-0 bottom-0 backdrop-brightness-50 backdrop-blur-sm">
    <div data-controller="modal" data-action="turbo:submit-end->modal#submitEnd keydown.esc@window->modal#hideModal" class='mx-auto mt-32 rounded-md p-8 w-96 break-words bg-white border shadow-xl grid gap-4'>
      <div class='flex justify-between items-start'>
        <h1 class="font-bold text-2xl"><%= @title %></h1>
        <button data-action="modal#hideModal"><span class='text-xl' style='color: transparent; text-shadow: 0 0 0 #EF4444;'>✖</span></button>
      </div>
      <%= content %>
    </div>
  </div>

<% end %>






